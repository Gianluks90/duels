/** Beta: obiettivi visibili in Collezione/Obiettivi/fine partita (catalogo, soglie, ricompense) ma
 * non ancora tracciati — nessuna partita conta per il progresso finché non si passa a `true`. Vedi
 * AuthService.applyGameStats, unico punto che la legge. */
export const OBJECTIVES_TRACKING_ENABLED = false;
