# Duels

> Archivio del contenuto completo di `README.md` al 2026-08-04, prima del secondo sfoltimento (il changelog "Qualità della vita" e "Achievements" era diventato troppo lungo da leggere per uso quotidiano) — changelog storico dettagliato, tenuto qui per riferimento. La versione corrente e alleggerita, con solo i punti ancora aperti, resta in `README.md` nella root del progetto. Per lo sfoltimento precedente (al 2026-07-21) v. [documentation/todo-archive-2026-07-21.md](todo-archive-2026-07-21.md).

## Qualità della vita

- [x] Loader alla board per il refresh.
- [x] Log eventi (azioni/danni/scarti consultabile a schermo).
- [x] Rivedere opzioni profilo utente — la dialog profilo è ferma alla v1 (nome, foto, dorso, elimina account); da rivedere/espandere.
- [x] Sfondo app e duello personalizzabili — permettere all'utente di scegliere/personalizzare lo sfondo dell'applicazione e quello della board di duello.
- [x] Conteggio turno anzichè "Turno di: nome-giocatore";
- [x] Magie preferite — un modo per il giocatore di segnare alcuni incantesimi come preferiti.
- [x] Magie pinnabili con tooltip in duel UI — poter "appuntare" un incantesimo durante il duello con un tooltip a schermo, per riferimento rapido senza dover riaprire il Grimorio. La stessa icona bookmark compare sugli elementi necessari a queste formule.
- [x] Regolamento — manca una pagina di glossario e una di glossario icone. Inoltre va controllato che tutto sia in linea con quanto sviluppato. Potrebbe essere anche bello includere le immagini di qualche carta per essere più esplicativi.
- [x] Amicizie - implementare aggiunta/rimozione amici.
- [x] Codici riscatto — dorsi carta sbloccabili tramite codice (`codes/{CODICE}` su Firestore, scrivibile solo da console/CLI, mai dal client), dialog "Riscatta codice" con anteprima del dorso sbloccato. Nella dialog profilo, il picker dorso è ora un componente a sé (`CardBackPickerComponent`, stesso schema selezione+Applica di `BackgroundPickerComponent` — niente più scritture su Firestore a ogni click), con griglia che va a capo e scorrimento indipendente per liste lunghe di dorsi. Corretto anche il dorso dell'avversario in game (mazzo, scarti, mano coperta): appariva identico al proprio invece che capovolto come se rivolto verso di lui.
- [x] Mana, seleziona tutto quando lanci una magia.
- [x] Varianti carte — l'utente potrà scegliere le varianti delle carte in gioco. Abbiamo due set completi di elementi: permettiamo all'utente di usare il suo preferito.
- [x] Collezione — nuova pagina (`/collection`) con dorsi/sfondi/titoli posseduti (varianti carte escluse, quella feature non esiste ancora — v. sezione "Achievements" sotto).
- [x] Correggere il modo in cui si svuotano le barre della vita: da invertire per giocatore e avversario.
- [x] Il comando "Crea" del grimorio diventa "Apprendi";
- [x] Condizione di vittoria (1.3) — il motore non determinava mai un vincitore (`GameState.winner` non veniva mai assegnato, `surrender()` portava lo status a `finished` senza deciderlo, la pagina risultato mostrava sempre l'esito neutro). Scoperto lavorando alla base dati stats per gli achievements: qualunque contatore vinci/perdi dipendeva da un campo mai popolato. Ora `resolveVictory`/`isGameOver` (`turn-engine.ts`) sono applicate centralmente da `GameEngineService.mutate()` dopo ogni reducer (hp <= 0 → vince l'altro); `surrender()` si è spostato da `GameService` a `GameEngineService` (ora fissa anche `winner`, non solo lo status); pareggio (entrambi <= 0 nello stesso reducer) resta senza winner, il regolamento non lo prevede.

## Achievements (obiettivi e ricompense)

Bozza obiettivi/ricompense in [documentation/achievements_ideas.md](achievements_ideas.md) — prime idee, da rifinire strada facendo.

Ordine di sviluppo consigliato (ogni punto dipende dal precedente):

- [x] Schema dati stats utente — `UserStats`/`EMPTY_USER_STATS` in `user.model.ts` (contatori cumulativi lifetime: vittorie, sconfitte, carte raccolte in Raccolta, combinazioni, incantesimi lanciati totali e per-incantesimo, danno/cura totali), aggiornati come delta calcolato dall'`eventLog` della partita appena conclusa.
- [x] Regole Firestore per l'integrità degli stats (`statsValid()` in `firestore.rules`) — pattern simile a `redeemGrantValid()` ma con un limite reale scoperto durante l'audit: le regole non contano occorrenze in `eventLog` (niente filtri/lambda su array), quindi solo partite giocate/vittorie/sconfitte sono validate ESATTAMENTE (dal solo `state.winner`), il resto solo per PLAUSIBILITÀ (delta non negativo, tetto largo). **Rischio accettato consapevolmente**: il guardiano anti-replay (`users/{uid}/countedGames/{gameId}`) blocca il replay solo attraverso il client reale, non è una garanzia crittografica (le regole non possono obbligare due scritture su documenti diversi ad avvenire insieme) — un client scritto ad hoc potrebbe gonfiarsi i propri stats, confinato al proprio account, ricompense cosmetiche. Una vera garanzia richiederebbe una Cloud Function (rimandata deliberatamente, prima volta che questo progetto ne avrebbe bisogno). Dettagli nei commenti di `firestore.rules`/`user.model.ts`.
- [x] `OBJECTIVE_CATALOG` (`data/objectives.ts`, mirror di `SPELL_CATALOG`) — ogni obiettivo come `metric + soglia + ricompense` (`Objective.rewards: ObjectiveReward[]`, `models/objective.model.ts` — un array: un obiettivo può dare più ricompense insieme, es. sia uno sfondo sia un titolo per lo stesso traguardo), non un campo dedicato per obiettivo. Contenuto dei reward (quale sfondo/titolo per quale obiettivo) volutamente **placeholder** (`id: 'TODO_...'`) — solo struttura per ora, i contenuti reali arriveranno quando decisi. Badge esclusi dal tipo `ObjectiveRewardType` (nessuna idea concreta al momento).
- [x] Estendere `UserProfile` per le nuove ricompense — `unlockedBackgrounds`, `title`/`unlockedTitles`, stesso schema equip+owned di `cardBack`/`unlockedCardBacks`; regole Firestore `backgroundValid()`/`titleValid()` (i 4 sfondi esistenti restano sempre gratuiti, solo quelli futuri saranno gated). **Varianti carte rimandate**: la selezione variante in sé non esiste ancora (è il punto "Varianti carte" ancora aperto in Qualità della vita) — aggiungere `unlockedCardVariants` ora significherebbe indovinare uno schema per una feature non ancora progettata, va ripreso insieme a quel punto.
- [x] Servizio di valutazione obiettivi a fine partita — `game/achievements.ts` (puro: `computeStatsDelta`/`applyGameStatsDelta`/`newlyCompletedObjectives`/`buildObjectiveProgress`, stesso spirito di `turn-engine.ts` vs `game-engine.service.ts`) + `AuthService.applyGameStats(gameDoc, role)` (Firestore I/O: scrive `stats`/`lastStatsGameId`/`completedObjectiveIds` + marker `countedGames/{gameId}` in un solo `writeBatch`). Chiamato da `ResultComponent` (un `effect()` nel costruttore) appena partita+ruolo sono noti — no-op sicuro da rieseguire (il marker lo impedisce). **Gap di integrità chiuso**: `AuthService.claimObjective(objectiveId)` + `claimValid()`/`claimGrantValid()` in `firestore.rules` validano che `claimedObjectiveIds ⊆ completedObjectiveIds` e che `unlockedBackgrounds`/`unlockedTitles` crescano solo insieme a un riscatto vero (max +1 a scrittura) — **lo stesso audit ha trovato e chiuso anche un gap gemello preesistente** su `redeemGrantValid()` (riscatto codice): validava solo che l'id sbloccato fosse presente in `unlockedCardBacks`, non che l'array non ne contenesse altri smerciati di soppiatto nella stessa scrittura.
- [x] UI fine partita — colonna achievement sulla pagina risultato (`ObjectiveCardComponent`, riusato anche dalla pagina Obiettivi): barra di progresso, si illumina quando completato, bottone "Riscatta" quando completato e non ancora riscattato.
- [x] Pagina Obiettivi (`/objectives`) — l'intero catalogo con il progresso dell'utente, stesso componente della colonna fine partita.
- [x] Pagina profilo pubblica (`/profile/:uid`) — raggiungibile dal proprio menu ("Il mio profilo") e da ogni riga della lista amici ("Vedi profilo"); mostra titolo (se sbloccato) + stats riassuntive. Nessuna distinzione tra "non amico" e "uid inesistente" in caso di lettura negata (stesso stato `notFound`), per non far scoprire a chi indovina un uid a caso se esiste davvero.
- [x] Header condiviso (`AppHeaderComponent`) — Home/Collezione/Obiettivi montano lo stesso header (navigazione continua, mai un "torna indietro" come unico modo per uscire); Grimorio/Regolamento/Opzioni si sono spostati dalla barra in alto (troppe voci cliccabili) a una colonna fissa di icon-button in basso a destra, stesso schema del `board__toolbar` nella Fonte Arcana durante il duello.
- [x] Pagina Collezione ridisegnata — un solo box unificato (header con titolo sempre in vista + corpo a due colonne indipendenti separate da un bordo, stesso schema di `RulebookDialogComponent`) invece di sezioni impilate. Dorsi/sfondi/titoli mostrano ora ANCHE gli slot non ancora sbloccati (non solo il posseduto): `ObjectiveRewardType` si è esteso con `'cardBack'` (prima solo sfondi/titoli potevano essere reward — richiedeva anche aggiornare `firestore.rules`, `unlockedCardBacks` ora cresce sia da riscatto codice sia da riscatto obiettivo) e `Objective` ha un nuovo campo `hidden?: boolean` per le condizioni di sblocco segrete ("???" invece di "Vittorie · 10", ma lo slot resta comunque visibile) — nessun obiettivo lo usa ancora, tutti restano a condizione visibile finché non deciderete quali rendere segrete. Ogni tile mantiene le dimensioni dello sblocco (lucchetto dorato via mask-image su `--text-heading`, stesso schema di `IconButtonComponent` — l'SVG fornito ha il fill chiaro hardcoded, non si tinge da `<img>`), tooltip con nome/descrizione/condizione al passaggio del mouse.
- [x] Dorsi carta: primo contenuto reale (non più placeholder) — `data/card-backs.ts` (`CARD_BACK_CATALOG`) cataloga i 6 dorsi esistenti (arte già in `public/cards-back/*.webp`), ciascuno col proprio meccanismo di sblocco: **dark/light** gratuiti; **golden** reward dell'obiettivo `win_10` (prima volta che un reward id non è un placeholder `TODO_...`; `ObjectiveCardComponent`/`CollectionComponent` risolvono il nome reale invece del generico "ricompensa da definire"); **books** da codice riscatto dedicato (il codice stesso non compare mai in UI); **founder** da acquisto sostenitori; **summer** da obiettivo stagionale. Questi ultimi tre sono mostrati in Collezione (il giocatore deve sapere che esistono) ma NON ancora sbloccabili davvero — v. "Casi limite" sotto.
- [x] Sfondi: stesso trattamento dei dorsi — `data/backgrounds.ts` (`BACKGROUND_CATALOG`) cataloga i 9 sfondi esistenti (arte già in `public/images/backgrounds/*.webp`, registrata in `backgrounds.json` con `free: false` per i 5 nuovi). Il meccanismo di sblocco (`RewardUnlock`, estratto in `models/reward-unlock.model.ts` e condiviso da dorsi/sfondi invece di due union duplicate) è: **dark-wood/light-wood/burn-wood/slate** gratuiti; **golden-fabric** reward di `win_50`; **felt-fabric** reward del nuovo obiettivo `played_50` (`gamesPlayed` · 50, non esisteva ancora); **arcane** reward di `cast_100`; **founder** da acquisto sostenitori (stessa condizione del dorso omonimo); **amber** da obiettivo stagionale (stagione autunnale, non ancora implementato). `collect_500`/`combine_50` restano placeholder (`TODO_...`), non menzionati per ora.
- [x] Titoli: meccanismo per le varianti di genere — bozza contenuti in `documentation/titles.md`, molti titoli richiedono forma maschile/femminile in italiano (+ una forma neutra/inclusiva con l'asterisco, es. "Istruit\*"). Scelta architetturale: nessun campo "genere" sull'account — `data/titles.ts` (`GENDERED_TITLE_IDS`/`titleRewardVariantIds`) fissa, per ogni titolo, quanti variant-id concedere (`<id>_m`/`<id>_f`/`<id>_x`, o un solo id invariante); `AuthService.claimObjective()` li accredita tutti insieme a `unlockedTitles`, e un nuovo `TitlePickerComponent` (dialog profilo, stesso schema selezione+Applica di `CardBackPickerComponent`) lascia scegliere liberamente quale forma equipaggiare — libertà totale, anche mista fra titoli diversi, senza dover dichiarare un genere una volta per tutte. `titleValid()` in `firestore.rules` resta invariata (continua a bastare `title in unlockedTitles`); solo il tetto di crescita per riscatto in `claimGrantValid()` è salito da +1 a +3 per i soli titoli. **Audit di follow-up**: il tetto più alto da solo avrebbe fatto gonfiare `unlockedTitles` più in fretta di dorsi/sfondi nello stesso rischio già accettato (nessun controllo che l'id concesso corrisponda DAVVERO al reward di quell'obiettivo) — aggiunto quindi un tetto assoluto `size() <= 200` a `claimedObjectiveIds`/`unlockedCardBacks`/`unlockedBackgrounds`/`unlockedTitles` in `claimGrantValid()`, stesso fix già applicato a `completedObjectiveIds` nell'audit precedente. `TranslationService.titleForms(baseId)` risolve le forme tradotte disponibili (`collection.titleCatalog.<variantId>.name`) — usato sia dall'anteprima ricompensa (`ObjectiveCardComponent`, tutte le forme separate da "/") sia dalla tile Collezione.
- [x] Titoli: primo contenuto reale — tutti i 15 obiettivi in `OBJECTIVE_CATALOG` hanno ora almeno un titolo vero invece di `TODO_...` (bozza in `documentation/titles.md`): **novice**/**apprentice** (primo duello/prima vittoria), **stubborn** (10 sconfitte), **gatherer**/**collector** (100/500 carte raccolte), **mixologist**/**alchemist** (10/50 combinazioni), **enchanter**/**magical**/**sorcerer** (10/50/100 incantesimi lanciati), **dangerous** (100 danni), **resilient** (100 cura). `collect_500`/`combine_50`/`cast_100` davano già uno sfondo: invece di scegliere quale reward vince, un obiettivo ora può dare più ricompense insieme (v. sopra), quindi danno entrambi. Genere: `novice`/`stubborn`/`gatherer`/`enchanter`/`magical`/`dangerous`/`sorcerer` sono in `GENDERED_TITLE_IDS` (coppie maschile/femminile italiane, es. "Novizio"/"Novizia", "Stregone"/"Strega"); `apprentice`/`mixologist`/`resilient`/`collector`/`alchemist` restano invarianti (parole di genere comune in italiano). Forma neutra/inclusiva (`_x`): ovvia per le coppie in -o/-a (sostituzione della vocale finale, "Novizi\*"), meno per le agentive in -tore/-trice e per la coppia irregolare `sorcerer` (`gatherer`→"Raccoglitor\*", `sorcerer`→"Streg\*") — convenzione scelta ma facile da rivedere, v. commento in `data/titles.ts`.
- [x] Titolo gratuito + titolo esclusivo — `TITLE_CATALOG` (`data/titles.ts`, nuovo, mirror di `CARD_BACK_CATALOG`/`BACKGROUND_CATALOG`: sostituisce la derivazione diretta da `OBJECTIVE_CATALOG` usata prima) elenca ORA tutti i titoli. **beginner** ("Principiante") è `unlock.kind === 'free'`, disponibile a chiunque fin da subito (`firestore.rules` ha una nuova `freeTitles()`, mirror di `freeCardBacks()`/`freeBackgrounds()`). **first_duelist** ("Primo duellante"/"Prima duellante") è invece `unlock.kind === 'exclusive'` a un singolo uid hardcoded (`EXCLUSIVE_TITLE_OWNER_UID` in `data/titles.ts`, stesso valore di `AuthService.DEBUG_UID` — l'account personale dello sviluppatore): nessun altro account può ottenerlo O equipaggiarlo — `exclusiveTitles()` in `firestore.rules` lo concede solo se `request.auth.uid` combacia — e `CollectionComponent.titleItems` lo esclude interamente dalla griglia per chiunque altro (non "bloccato e visibile" come ogni altro titolo, proprio assente). Resta comunque visibile normalmente ovunque un titolo equipaggiato si mostri (profilo pubblico) per chi guarda il profilo di quell'uid: l'esclusività riguarda solo chi può OTTENERLO, non chi può VEDERLO una volta equipaggiato — `ProfileComponent.displayTitle` risolve sempre `p.title` (una variant-id) in testo vero via `TranslationService.titleLabel`, senza restrizioni di visualizzazione. `TitlePickerComponent`/`ProfileDialogComponent` risolvono le variant-id equipaggiabili senza riscatto per l'utente corrente con `freeTitleVariantIdsFor(uid)`.
- [x] Titolo di default + titolo in game + picker a select — nuovi account (`AuthService.ensureUserProfile`) partono ORA con `title: 'beginner'` già impostato, non più assente finché non se ne sblocca uno. Il titolo equipaggiato è visibile anche durante il duello, non solo su profilo/collezione: `GameDoc.hostTitle`/`guestTitle` (nuovi campi, stesso schema snapshot-al-join di `hostFavoriteSpellIds`/`hostCardBack` — nessuna regola aggiuntiva in `firestore.rules`, non sono validati per campo più di quanto già lo siano gli altri) portano la variant-id nel documento partita, `BoardComponent.playerTitle`/`opponentTitle` la risolvono in testo con `TranslationService.titleLabel` e la passano a un nuovo input `equippedTitle` di `PlayerHudComponent` (chiamato così, non `title`, per non confonderlo con l'attributo HTML nativo) — mostrato in una riga sotto il bordo del nome, allineata allo stesso lato (sinistra/destra per proprio/avversario, mirror di `.hud__identity`), raggruppata con `.hud__name-row` dentro un unico `.hud__header` per non alterare lo `space-between` fra intestazione e barra vita. `TitlePickerComponent` è passato da pillole a un `<select>` (`.title-picker__select`, stessi valori di `.background-picker__select`/`.options-dialog__select` — un dropdown deve leggersi uguale ovunque nell'app): con le varianti di genere le opzioni possono diventare tante, un menu a tendina scala meglio di una griglia di pillole.
- [x] Dorso **friendship** aggiunto al catalogo (`{ id: 'friendship', unlock: { kind: 'inviteFriend' } }`) e nuovo `RewardUnlock.kind === 'inviteFriend'` (stesso trattamento di `'purchase'`/`'seasonal'`: mostrato in Collezione, nessun meccanismo reale ancora — l'invito-amico che lo sbloccherà non esiste come feature).
- [x] Riassegnati i dorsi di `win_10`/`win_streak_5` (`documentation/achievement-titles.md`) — **golden** passa da `win_10` a `win_streak_5` (si affianca al Titolo "Inarrestabile" già lì: primo caso nel catalogo dove un obiettivo dà sia un Titolo sia un Dorso insieme). `win_10` eredita **light**, che smette di essere gratuito (`freeCardBacks()` in `firestore.rules` ora `['dark']`, non più `['dark', 'light']`; mirror in `public/config/card-backs.json`). **Rottura deliberata e consapevole** del principio "il gratuito resta gratuito per sempre" ancora valido per `freeBackgrounds()` — accettata perché non toglie nulla a chi ha già `light` equipaggiato (`cardBackValid()` valida solo quando il campo CAMBIA), ma chi lo cambierà in futuro dovrà riguadagnarlo vincendo 10 duelli; nessuna migrazione retroattiva su `unlockedCardBacks` per gli account esistenti.

Effetto collaterale scoperto costruendo il servizio di valutazione: `cardsCollected` (carte raccolte in Raccolta) non aveva NESSUNA voce nell'`eventLog` da cui derivarlo (a differenza di incantesimi/combinazioni/danno/cura, già tutti loggati) — aggiunta una nuova voce `cardCollected` (solo su `keepCard`, non su `keepMana`: nessuna carta vera entra nel mazzo in quel caso) apposta per questo. Esclusa di proposito dalla vista del Log di gioco (`GameLogDialogComponent`, troppo frequente — una volta a turno — per essere un evento "notevole" lì).

### Obiettivi ancora da implementare, chiusi in ordine di complessità crescente

Nessuna `UserStats`/campo li misurava all'inizio (richiedevano nuovi contatori/meccaniche prima di poter entrare in `OBJECTIVE_CATALOG`, elencati insieme ai titoli omonimi in `documentation/titles.md`) — tutti ora completati:

- Bassa complessità (un solo campo nuovo, nessun tocco al motore di gioco):
  - [x] Login 7 giorni consecutivi — `UserProfile.lastLoginDate`/`loginStreak` (calendario locale, `nextLoginStreak` in `game/achievements.ts`), aggiornato ad ogni avvio app (`AuthService.ensureUserProfile`) fuori dal flusso di fine partita.
  - [x] "Inarrestabile" (5 vittorie consecutive) — `UserStats.currentWinStreak`, +1 su vittoria/azzerato su sconfitta/invariato su pareggio, derivabile esattamente da `winner` come wins/losses (v. `winStreakValid()` in `firestore.rules`).
  - [x] "Istruito"/"Istruita" (aver letto tutto il regolamento) — `UserProfile.rulebookRead`, segnato da `RulebookDialogComponent` quando ogni sezione di `regolamento.json` è stata aperta almeno una volta.
- Media complessità (riusano pattern già esistenti nel codebase, ma toccano più file):
  - [x] "Spezzadifese"/"Distruttore" (scudi rimossi) — soglie riviste a 10/50 (non 1/100: si conta il VALORE rimosso, non i cast, v. sotto). `UserStats.shieldsRemoved`, log esplicito dentro il reducer (`GameLogEntryData.shieldRemoved`, `turn-engine.ts` `case 'shield_remove_opponent'`) con l'`amount` REALE rimosso (Breccia azzera tutto lo scudo presente, un valore variabile), non derivabile a posteriori dal diff di stato. Aggiunti anche "In difesa"/"In guardia"/"La muraglia" (10/50/100 scudo GUADAGNATO, `UserStats.shieldsGained`) riusando `shieldGained`, già loggato.
  - [x] "Amichevole"/"Rivale" (duello con un amico, 5 vittorie contro un amico) — `GameDoc` non registra se host e guest erano amici al momento della partita.
  - [x] "Oscuro"/"Oscura"/"Luminoso"/"Luminosa" (combinare 5 volte un Elemento Potente Oscurità/Luce) — nuovo `UserStats.elementsObtained` (contatore unico per elemento, alimentato da `GameLogEntryData.cardCollected`/`combined`: i 4 base si raccolgono in Raccolta, gli altri 7 nascono sempre da una combinazione, mai le due fonti sullo stesso elemento). **Bonus non preventivato**: lo stesso contatore ha sbloccato anche le 11 "Variante 'Elemento X'" (soglia 25, un obiettivo per `COLLECTIBLE_ELEMENT_IDS`) — nuovo `ObjectiveRewardType.elementVariant` + `UserProfile.unlockedElementVariants`, riattivate le tile arte v1 in Collezione (`CollectionComponent.elementItems`, prima sempre bloccate con "nessun meccanismo deciso").
  - [x] "Che tutto vede" (i 3 incantesimi di rivelazione posseduti insieme) — reinterpretato invece di loggare l'istante esatto: nuovo `CARD_PATTERN_CATALOG` (`data/card-patterns.ts`, `models/card-pattern.model.ts`) valuta lo stato FINALE del mazzo del giocatore (mazzo residuo + mano + scarti + carte "in transito" — `pendingCollect`/`pendingSpells` — v. `computeCardPatternMatches` in `game/achievements.ts`) invece dell'`eventLog`: una carta-incantesimo, una volta creata, non sparisce mai, quindi "le hai ancora a fine partita" è verificabile senza il rischio di cap a 50 voci che limitava l'approccio originale. Meccanismo generico e riusabile — quattro modalità (`contains`/`exclusiveAll`/`exclusiveAny`/`excludes`) e un `cardKind` ('spell' o 'element', le carte dell'altro genere non contano nelle modalità esclusive: le magie create non "rompono" un pattern sugli elementi) pensato anche per obiettivi futuri come "solo carte Fuoco e Lava nel mazzo" (`exclusiveAll`, non ancora nel catalogo). Nuovo `UserStats.cardPatternMatches` (mappa libera per pattern, stesso schema di `spellCastCounts`, validata solo per forma/dimensione in `firestore.rules`), nuove chiavi `ObjectiveMetric` `pattern_<id>` generate dal catalogo, nuova categoria "Mazzo" nella pagina Obiettivi. Reward ancora `TODO_reveal_trio` (titolo), da decidere con lo stesso processo di `documentation/achievement-titles.md`.
- Complessità medio-alta (nuovo stato persistente o logica causale nel motore):
  - [x] Ripensato come contatore LIFETIME invece che "10 volte in un duello" (stesso rischio-cap di "Che tutto vede" prima della sua reinterpretazione, ma qui non c'era uno stato finale persistente da leggere: le carte Congelamento si sciolgono, non restano nel mazzo) — nuovi eventi `freezeApplied`/`poisonApplied` in `GameLogEntryData` (loggati da `applyFreeze`/`applyPoison` in `turn-engine.ts` con l'`amount` NOMINALE dell'incantesimo, non un valore limitato dal cap di `PlayerTokens.poison`), nuovi `UserStats.freezeApplied`/`poisonApplied`, derivati dall'eventLog come `shieldsGained`/`shieldsRemoved` (stesso rischio-cap già accettato per quei due, qui accettabile perché sono contatori lifetime su tante partite, non un singolo picco da 10 in una sola). Quattro nuovi obiettivi: `freeze_50`→titolo "...della neve", `freeze_100`→"...del ghiaccio", `poison_50`→"l'avvelenatore/l'avvelenatrice/l'avvelenator\*", `poison_100`→"...della Pestilenza".
  - [x] "Vipera" (vittoria con ultimo danno da veleno) — più semplice di quanto sembrasse: `DamageLogSource` (`game-log.model.ts`) tagga già ogni voce `damage` con `{kind:'spell'|'explosion'|'poison'}`, e la vittoria si decide subito dopo ogni reducer (`hp <= 0`) — quindi basta l'ULTIMA voce `damage` sul perdente in un duello vinto (`wonWithPoisonFinish` in `game/achievements.ts`, scandisce l'eventLog a ritroso). Nuovo `UserStats.poisonFinishWins` (max 1 a partita), nessuna modifica al motore.
  - [x] Categoria "Bacchetta" (bassa complessità, `documentation/achievement-titles.md`) — `wandTipHeld`/`wandSocketed` erano già eventi loggati (per il Log di gioco) ma mai letti da nessun achievement: ora alimentano `UserStats.tipHeld`/`bodySocketed`/`handleSocketed` con lo stesso schema di `combinationsMade`/`spellsCast`. 7 nuovi obiettivi: `wand_tip_10`/`wand_tip_50` (Previdente/Lungimirante), `wand_body_10`/`wand_body_50` (Temprato/Inespugnabile), `wand_handle_10`/`wand_handle_50` (Incantato/Magnetico), `wand_actions_300` (metrica derivata `wandActionsTotal = tipHeld + bodySocketed + handleSocketed`, Titolo "Maestro di bacchetta" — il dorso "wands" resta invece su `wand_self_vulnerable`, non ancora implementato all'epoca, poi completato). `bodySocketed`/`handleSocketed` limitati a max 1/partita in `statsPlausible()` (`firestore.rules`): lo slot si riempie una sola volta, mai sovrascrivibile.
- Alta complessità (serve prima una decisione di design, poi codice cross-sistema):
  - [x] "Preparato a tutto"/"Preparata a tutto" — stesso meccanismo di "Che tutto vede" sopra: nuovo pattern `all_elements` in `CARD_PATTERN_CATALOG` (`cardKind: 'element'`, modalità `contains`, i 4 base + 4 avanzati + luce/tenebra — Residuo Arcano/mana esclusi apposta, non menzionati nel design originale, ma non "rompono" comunque il pattern in modalità `contains`), nuovo obiettivo `all_elements`, nuovo titolo `prepared` (gendered, "Preparato a tutto"/"Preparata a tutto"/"Preparat* a tutto").
  - [x] "Diabolico"/"Diabolica" (vittoria usando Fiamma Nera) — nuovo pattern `black_flame_win` in `CARD_PATTERN_CATALOG` (`mode: 'contains'`, `requireWin: true`): una carta incantesimo lanciata finisce negli scarti del GIOCATORE, non in quelli comuni (`resolveSpells` in `turn-engine.ts`), quindi resta rintracciabile nello stato finale come "Che tutto vede"/"Fortunato" — nessuna modifica al motore, nessun rischio-cap sull'eventLog.
  - [x] "Infernale"/"Corazzato"/"Distruttibile" — nuovo evento nel motore `wandResistanceTriggered` (`GameLogEntryData`, loggato da `bodyResistanceOutcome`/`logWandResistance` in `turn-engine.ts` sui tre effetti danno "normali": `damage`/`damage_ignore_shields`/`damage_self`) con `outcome: 'resisted' | 'vulnerable'` e `selfInflicted: boolean` — prima la Resistenza/Vulnerabilità dell'asta scattava senza lasciare traccia nel log, indistinguibile da uno scudo che assorbe. Nuovi `UserStats.wandDamageResisted` (qualunque fonte, "Corazzato"/"Indistruttibile" 10/50) e `selfDamageResisted` (sottoinsieme solo auto-inflitto, "Infernale" soglia 1 — oggi possibile solo con Fiamma Nera, l'unico incantesimo con `damage_self`). Anche visibile nel Log di gioco (`GameLogDialogComponent`).
  - [x] "Temerario"/"Temeraria"/"Non temo nulla" — chiude la categoria "Bacchetta": riusa lo stesso evento `wandResistanceTriggered` sopra (`outcome === 'vulnerable' && selfInflicted`, oggi Fiamma Nera con Acqua incastonata) combinato con `winner`, come "Diabolico" — nuovo `UserStats.selfVulnerableWins`/`wonWithSelfVulnerable` in `game/achievements.ts` (a differenza di "Vipera" non serve l'ULTIMA occorrenza, basta che sia successo in un turno qualunque di una partita poi vinta). **Unico obiettivo del catalogo il cui dorso premia un azzardo deliberato invece di grind/completismo**: nuovo dorso **wands** (`data/card-backs.ts`, asset già pronto in `public/cards-back/wands.webp`) come reward insieme al Titolo, non spostato da `wand_actions_300` come discusso in origine (v. `documentation/achievement-titles.md`).
  - [x] "Fortunato / Fortunata / Fortunat\*" — stesso pattern-mazzo di "Che tutto vede"/"Preparato a tutto" sopra, con due aggiunte al modello (`CardPattern`, card-pattern.model.ts): `allowedExtra` (identificativi tollerati oltre a quelli richiesti, senza romperne l'esclusività — qui le magie base, comprese `starter_bolt`/`starter_balm` sempre presenti fin dall'inizio: escluderle avrebbe reso l'obiettivo impossibile) e `requireWin` (il pattern combacia solo se il duello è stato VINTO in quelle condizioni, non semplicemente giocato — `computeCardPatternMatches`/`matchesPattern` in `game/achievements.ts` ricevono ora anche `won`). Pattern `lucky_win`: Reset E Rischio sempre entrambi richiesti, nessun'altra magia craftata ammessa.

## Reward `TODO_...` di `collect_500`/`combine_50` risolti

- [x] Titolo "Collezionista" scartato per `collect_500` (rimandava troppo alla pagina Collezione, non alla Raccolta 4.3 che l'obiettivo misura) — nuovo obiettivo intermedio `collect_250` ("Archivista", invariante di genere) riempie il salto 100→500 con una curva più graduale (2.5x→2x, in linea con `cast_15/50/100`). Gli sfondi/dorsi `TODO_...` di `collect_500`/`combine_50` sono stati risolti riassegnando due asset reali già pronti ma orfani invece di produrne di nuovi: lo sfondo **amber** (`public/images/backgrounds/amber.webp`, era `unlock.kind: 'seasonal'` senza alcun obiettivo che lo consumasse) è passato a `collect_500`; il dorso **amber** (`public/cards-back/amber.webp` esisteva su disco ma non era mai stato catalogato in `CARD_BACK_CATALOG`) è diventato il reward di un nuovo obiettivo `combine_150`, un gradino oltre "Alchimista" (`combine_50`, che ora dà solo il titolo). Stesso schema già usato per il dorso Dorato spostato da `win_10` a `win_streak_5`. Dettagli in `documentation/achievement-titles.md`.

## Esplosione elementale (2.4) — rimossa

- [x] Rimossa del tutto l'Esplosione elementale (2.4): risultava in conflitto con l'incantesimo "Migliora mana" (`improve_mana`, formula Luce+Tenebra) — creare quell'incantesimo richiede avere Luce e Tenebra contemporaneamente in mano per scartarle in pagamento (`createSpell`, formula), ma `resolveElementalExplosions` risolveva l'esplosione nello stesso istante in cui la seconda carta entrava in mano (agganciata a ogni transizione di stato), prima che il giocatore potesse mai aprire il Grimorio — un blocco totale, non solo una sfortuna rara. Rimossi `ExplosionEvent`/`ExplosionLocation`/`explosionBatchId`/`lastExplosions` (`game.model.ts`), `resolveElementalExplosions` e i suoi 5 punti di chiamata (`combineElements`/`combineSuperior`/`endTurn`/`resolveSpells`/`tryStartGame`/`createDebugGame`) in `turn-engine.ts`/`game-engine.service.ts`/`game.service.ts`, gli eventi `handExploded`/`fonteExploded` (`game-event.model.ts`/`derive-events.ts`), tutta l'animazione "lampo+scossa" dedicata (`AnimationQueueService.handExplosions`/`fonteExploding`, le relative `@keyframes hand-card-explode`/`fonte-explode` in `board.component.scss`) e il kind `'explosion'` di `DamageLogSource` (`game-log.model.ts`, mai più prodotto: solo `'spell'`/`'poison'` restano). Regolamento aggiornato (`rules.md` 2.4, `elements.md`/`glossary.md`/`mana.md` in-app) — la sezione 2.4 ora descrive solo "Elemento potente", non più "ed Esplosione elementale".
- [x] Valore mana di Luce/Tenebra alzato da 2 a 3 (`ELEMENT_MANA`, `element.model.ts`) — un gradino sopra gli elementi avanzati (2), a riflettere che servono 4 basi da combinare (2.4) contro le 2 di un avanzato (2.3), quindi sono più rari/costosi da ottenere. Rulebook aggiornato di conseguenza (`rules.md`, `elements.md`, `mana.md`).

### Caveat noti (accettati, non bloccanti)

- `GameState.eventLog` è tagliato alle ultime 50 voci (v. `game.model.ts`) — rischio per obiettivi "dentro una singola partita" se la partita è molto lunga; accettato caso per caso quando un obiettivo si basa sull'eventLog invece che sullo stato finale del mazzo.
- `completedObjectiveIds`/`claimedObjectiveIds`/`unlockedCardBacks`/`unlockedBackgrounds`/`unlockedTitles` sono validati per struttura/monotonia/tetti di crescita (v. `firestore.rules`), ma NON verificano che gli id riscattati corrispondano davvero ai reward di QUELL'obiettivo in `OBJECTIVE_CATALOG` — richiederebbe incorporare l'intero catalogo nelle regole. Stesso rischio accettato consapevolmente di `statsPlausible()`.
