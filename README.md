# Duels

## Prossimi passi (in ordine di priorità)

Consolidato qui il lavoro ancora aperto, sparso tra le varie sezioni storiche sotto — quelle restano come changelog dettagliato, questa lista è il punto di partenza per decidere cosa affrontare dopo.

1. **Incantesimi (regolamento v2, sez. 5)** — un v1 minima esiste già ed è giocabile: `castSpell` (Azione, paga subito scartando le carte indicate — `CastSpellDialogComponent`) mette la carta in sospeso in `PlayerState.pendingSpells`, `resolveSpells` (agganciata dentro `advanceTurnPhase`, al passaggio in fase Incantesimo) applica gli effetti. Di 18 `SpellEffectType` nel catalogo (`src/app/data/spells.ts`), 17 sono davvero risolti in `applySpellEffect`: `damage`/`heal` da sempre; `poison_add`/`ice_add` aggiunti l'11/07/2026 insieme alle prime magie di ghiaccio (frost/blizzard/ice_age) — usano `applyPoison`/`applyFreeze`, già scritte in `turn-engine.ts` ma rimaste senza chiamante per mesi; `shield_add`/`damage_ignore_shields` aggiunti lo stesso giorno insieme a protection/wall/aegis (scudo su di sé, `applyShield`) e lightning_bolt (danno che buca lo scudo) — il case `'damage'` ora assorbe dallo scudo prima dell'hp (`absorbWithShield`), prima non lo faceva pur avendo tutta la UI già pronta; `damage_self`/`poison_clear_self`/`ice_clear_self` aggiunti lo stesso giorno con black_flame (danno anche a sé, stesso spellElement 'fire' per entrambi i bersagli), heat e detox (le due "cure di sé" — `applyIceClear` pulisce solo gli scarti del lanciatore, non hand/deck: una carta 'freeze' già altrove si scioglie comunque da sola in Preparazione); `shield_remove_opponent`/`damage_halve_opponent`/`opponent_discard_random`/`fonte_reset` aggiunti il 12/07/2026 con fracture/breach (`applyShieldRemove`, con `amount` assente su breach = azzera tutto lo scudo invece di sottrarne una quantità fissa, stesso schema "amount assente = tutto" delle due `_clear_self` sopra), black_hole (dimezza l'hp corrente dell'avversario, `Math.floor(hp/2)`, fuori dal circuito scudo/asta di proposito), violent_gust (`applyDiscardRandom`, esclude di proposito tier `'residium'`/`'mana'` dal finire negli scarti — si consumano invece, stesso trattamento che ricevono ovunque altrove nel motore) e reset (`applyFonteReset`, l'unico effetto che tocca lo stato condiviso del tavolo — Fonte Arcana — invece che quello di un giocatore); `opponent_discard_hand`/`reveal_opponent_hand` aggiunti lo stesso giorno con low_blow (`applyDiscardHand`, stesso ciclo scarta-mano+pesca-5 di `endTurn`, isolato dal resto degli effetti collaterali del cambio turno) e third_eye/supreme_eye (`applyRevealHand`, nuovo `Card.revealedToOpponent` — permanente sulla carta stessa finché non "guarita", nessun incantesimo lo fa ancora — invece del vecchio scaffolding a livello giocatore `PlayerState.handRevealed`/"Occhio del Sole", mai collegato: rimasto lì come scaffolding morto); `damage_from_fonte` aggiunto il 12/07/2026 con risk (formula = le stesse 4 basi di `combineSuperior`, nessun elemento portante — `countAdvancedPairsInFonte` in `turn-engine.ts` conta le coppie di elementi avanzati uguali tra i 4 slot rivelati in Fonte Arcana in quel momento, 3 danni a coppia, 0-6 totali; Luce/Tenebra escluse dal conteggio perché esploderebbero all'istante se compresenti, 2.4, quindi non restano mai abbastanza a lungo da formare una coppia osservabile — mana basso apposta, l'esito è spesso 0); `boost_card_mana` aggiunto lo stesso giorno con improve_mana — prima magia con una carta bersaglio scelta dal giocatore invece di un target cablato (`TARGET_CARD_EFFECT_TYPES` in `spell.model.ts`), vedi voce dedicata sotto. Anche `resolveSpells` ora richiama `resolveElementalExplosions` in coda (mancava — un incantesimo che pesca carte fresche in una mano o rigenera la Fonte Arcana può introdurre Luce+Tenebra insieme, come endTurn/combineElements già gestivano). Resta 1 solo `SpellEffectType` no-op: `element_immunity`, deliberatamente rimandato (vedi sotto), non "ancora da fare" — `opponent_skip_collect` è stato invece rimosso del tutto dall'unione (scartato, non solo rimandato, vedi sotto). Tuono/Lava (elementi avanzati a sé, non in questa enum) restano bloccati sullo stesso motivo (2.3.2/2.3.3, vedi punto 3 sotto) — nota: lightning_bolt/black_flame/fracture/breach usano già Tuono/Lava come ingredienti di formula, ma non introducono ancora le rispettive meccaniche speciali (2.3.2 "torna in fondo al mazzo", 2.3.3 "consuma carte"): restano incantesimi normali a scarto.

   **UI Occhio (Terzo occhio/Occhio supremo)**: `CardComponent` ha un nuovo input `revealedToOpponent` — gettone grigio in colonna sotto il mana speciale (badge condiviso con esso, in coda se entrambi presenti sulla stessa carta, `.card__badge--revealed-after-special`), solo visivo + aria-label (niente tooltip proprio: quello ricco è responsabilità del chiamante, stesso schema di `specialMana`, per poter convivere con gli altri tooltip già gestiti lì). Propagato ovunque una carta reale possa comparire: mano propria, mano avversario (novità — vedi sotto), pile-dialog, cima degli scarti (`DeckComponent`, nuovo `topRevealedToOpponent`). Tooltip ricco (titolo+icona su una riga, poi `<hr>`, poi descrizione — stesso formato di `specialManaTip`) aggiunto in `board.component.html`/`pile-dialog.component.html` ovunque una carta vera sia ispezionabile; quando più tooltip si applicano alla stessa carta (in pratica solo mana speciale + Occhio, dato che spell/freeze/mana-token sono già mutuamente esclusivi per tier) vengono affiancati in colonna (`.rich-tooltip__multi`/`.rich-tooltip__multi-item`) invece di sceglierne uno solo — logica centralizzata in `cardTooltipFlags`/`hasCardTooltip`/`isMultiCardTooltip`, duplicata in entrambi i componenti (nessun mixin condiviso nel progetto). La vera novità è nella mano dell'avversario in `board.component.ts`/`.html`: prima renderizzava SEMPRE dorsi generici a prescindere dal contenuto (l'unica eccezione era il lampo istantaneo dell'esplosione elementale) — ora itera sulle carte vere (`opponentHand()`, il dato in realtà è sempre stato leggibile dal client, semplicemente non ancora consultato lì) e mostra la carta reale al posto del dorso solo per quelle con `revealedToOpponent` true, stessa posizione nel ventaglio di prima (`handCardStyle` non distingue dorso/carta vera, solo indice), coi 5 tooltip ricchi al completo.

   **UI carta bersaglio (Migliora mana)**: prima magia il cui effetto agisce su una carta scelta dal giocatore, non un target cablato — nuovo `SpellEffect` union member `boost_card_mana` e `TARGET_CARD_EFFECT_TYPES` (`spell.model.ts`) che elenca quali tipi lo richiedono (oggi solo questo). `PendingSpell.targetCardId` porta la scelta da `castSpell` (Azione) fino a `resolveSpells` (Incantesimo) — validata al lancio: deve essere un elemento reale (base/avanzato/potente) ancora in mano, non la carta incantesimo né una delle carte scelte per pagare, e **non** la punta della bacchetta (a differenza del pagamento, che la include: il bersaglio deve restare "in mano" in senso stretto). `CastSpellDialogComponent` ha ora una seconda lista opzionale (mostrata solo se `needsTarget()`), selezione singola invece del multi-select del pagamento, evidenziata in blu invece che oro per distinguerla a colpo d'occhio; scegliere una carta come pagamento la toglie automaticamente dalla lista bersaglio (e viceversa non serve, la lista bersaglio esclude già le carte-pagamento in modo reattivo). `improve_mana` (Luce+Tenebra) tenuta a 2 mana invece dei 3 proposti inizialmente: con mano da 5 carte, spell card + pagamento consumano già fino a 3 carte a 2 mana (basi da 1 l'una) — a 3 mana il pagamento da solo poteva arrivarci, lasciando una sola carta rimasta in mano come bersaglio, una "scelta" solo nominale.

   **`opponent_skip_collect`/`element_immunity` — deliberatamente fuori scope** (deciso il 12/07/2026):
   - `opponent_skip_collect` — **rimosso del tutto** dall'unione `SpellEffectType` (non solo rimandato): negare la Raccolta dell'avversario (l'unico modo per accumulare risorse ogni turno) rischia di creare uno stallo invalicabile — un giocatore denied ripetutamente non ha contropartita oggi (nessuna immunità, nessun limite a quante volte un incantesimo si può rilanciare nella partita). Da riconsiderare eventualmente con un vincolo forte (es. una tantum a partita, o un minimo garantito), non come effetto libero.
   - `element_immunity` — non rimosso, solo rimandato: richiede un vero sistema di stati a scadenza sul giocatore (non sulla singola carta, come `Card.expiresAt`), che oggi non esiste. Ripreso come parte di una futura espansione dedicata, "Status e magie continue".
   - `PlayerState.handRevealed`/"Occhio del Sole" e `immuneToElement`/"Abbraccio Radiante" — questi invece **rimossi** il 14/07/2026 (non solo rimandati come `element_immunity` sopra): scaffolding mai collegato a nulla (`handRevealed` superato da `Card.revealedToOpponent`, `immuneToElement` mai un vero sistema a scadenza). Da ricostruire da zero, con la forma giusta, quando si affronterà "Status e magie continue" — non riutilizzabili così come erano. Stessa sorte per `PlayerTokens.ice`: mai letto né scritto, il Congelamento (2.3.1) è implementato interamente con vere carte `tier: 'freeze'`, non un token sul giocatore.

   Con questi due fuori dallo scope attivo, il v1 degli Incantesimi si considera completo lato `SpellEffectType` — 17/18 risolti, l'ultimo (`element_immunity`) in pausa volontaria in attesa della sua espansione dedicata.

   Questa voce non era mai stata aggiornata qui nonostante il codice fosse già a questo punto — disallineamento notato e corretto il 2026-07-08. Aggiornata più volte l'11/07/2026: prima con `poison_add`/`ice_add` (frost/blizzard/ice_age), poi con `shield_add`/`damage_ignore_shields` (protection/wall/aegis/lightning_bolt) e la scoperta di `PlayerState.handRevealed`/`immuneToElement` (scaffolding già pronto, mai documentato qui prima), infine con `damage_self`/`poison_clear_self`/`ice_clear_self` (black_flame/heat/detox). Aggiornata più volte anche il 12/07/2026: `shield_remove_opponent`/`damage_halve_opponent`/`opponent_discard_random`/`fonte_reset` (fracture/breach/black_hole/violent_gust/reset), poi `opponent_discard_hand`/`reveal_opponent_hand` (low_blow/third_eye/supreme_eye, più il nuovo `Card.revealedToOpponent` e la UI mano-avversario che lo mostra, poi rifinita con tooltip ricco esterno e supporto multi-tooltip), poi `damage_from_fonte` (risk) — 16 `SpellEffectType` risolti — infine deciso di rimandare i 2 rimasti (`opponent_skip_collect` scartato, `element_immunity` rimandato a "Status e magie continue").
2. ~~Esplosione elementale (2.4)~~ — fatto, vedi sez. "Meccaniche degli elementi" sotto.bug
3. **Tuono (2.3.2) e Lava (2.3.3)** — Tuono deciso il 14/07/2026: ignora gli scudi del bersaglio (`lightning_bolt`/`damage_ignore_shields` già implementa esattamente questo, il regolamento è stato solo allineato al codice esistente). Lava resta da specificare: manca ancora un `SpellEffectType` per "consumare carte dal proprio mazzo per alleggerirlo" (2.3.3) — oggi solo la metà scudo (`shield_add`) è coperta.
4. ~~Mana speciale (3.2.x)~~ — fatto, vedi sez. "Meccaniche degli elementi" sotto.
5. ~~Dialog di scelta per "Combina"~~ — fatto, vedi sez. "Meccaniche degli elementi" sotto.
6. ~~Tooltip mana speciale sulla carta~~ — fatto, vedi sez. "Meccaniche degli elementi" sotto.
7. **Punta della bacchetta a runtime** — trascinare un elemento sulla punta per conservarlo al turno successivo (1.4.1); l'effetto è già descritto in UI (`board.wand.tipEffect`) ma non ancora agganciato a nessuna azione.
8. **Hardening regole Firestore** — oggi qualsiasi utente autenticato può scrivere lo stato di qualsiasi partita (accettato finora, non bloccante per il gameplay, ma da chiudere prima di un rilascio più ampio).
9. **Polish minori, indipendenti tra loro**: loader alla board per il refresh (componenti vuoti/distorti finché non arriva il primo stato, sez. Note aperte), etichetta "Bacchetta comune" larga quanto le tre sezioni, log eventi (azioni/danni/scarti consultabile a schermo).
10. **Debito tecnico**: l'`effect()` di auto-avanzamento per l'avversario di debug in `board.component.ts` va rimosso/sostituito appena esiste un modo vero di testare con un secondo client reale (o un'IA vera) — bloccato su quello, non su altro.
11. **Bassa priorità, solo da tenere a mente**: valutare in futuro "scarta invece di subire danno" come alternativa al danno automatico dell'esplosione elementale (punto 2), se il playtest lo suggerisce.
12. **Nuovi incantesimi con effetti già esistenti** — emersi dalla review di bilanciamento del 14/07/2026 (vedi `documentation/rulebook/v2/spells.md`), da valutare *dopo* aver chiuso il bilanciamento dei costi mana attuali (punto 1 sopra e voci correlate):
    - ~~Cura craftabile~~ — fatto il 14/07/2026: `mend` (Acqua+Aria, diff2, +2 cura) e `radiant_heal` (Luce+Acqua, diff5, +3 cura). Solo 2 livelli apposta, non una scala a 3 — troppa cura craftabile rischia di far stagnare la partita.
    - ~~Tuono sottorappresentato~~ — fatto il 14/07/2026: scala completata a 3 livelli, `spark` (Tuono+Aria, diff3, 2 danni ignora scudi) e `thunderstorm` (Tuono+Tuono+Tenebra, diff8, 5 danni ignora scudi) affiancano `lightning_bolt`.
    - **`reveal_opponent_hand` solo di fascia alta** — non risolto come originariamente pensato (una versione economica base/avanzato): deciso invece il 14/07/2026 di aggiungere `spell_glimpse` (Luce+Fuoco, stessa fascia di `third_eye`), che rivela specificamente una magia in mano all'avversario invece di una carta qualunque. Richiesto un piccolo campo in più, `SpellEffect.cardTierFilter` (spell.model.ts) — `applyRevealHand` in turn-engine.ts ora pesca solo tra le carte del tier indicato, no-op se il bersaglio non ne ha. `third_eye`/`supreme_eye` lasciati invariati.
    - ~~`opponent_discard_random` ha un solo incantesimo~~ — fatto il 14/07/2026: `dark_gust` (Aria+Tenebra, diff5, mana4, scarta 2 carte casuali) affianca `violent_gust`. Fermato a 2 (non 3) di proposito: su una mano da 5 scarterebbe più della metà in un colpo solo.
    - **`damage_self` usato una sola volta** (`black_flame`) — lasciato così per ora (deciso il 14/07/2026): l'idea è tematizzarlo meglio quando si introdurranno le scuole di magia come categoria narrativa, che permetterebbero incantesimi meccanicamente identici ma giustificati da formula/scuola diversa invece che da un effetto nuovo.

## TODO (storico)

Riorganizzata per priorità: prima le fondamenta da cui dipendono altri task, poi il gameplay core, poi i18n/opzioni, infine il profilo utente.

### 1. Quick win

- [x] Board: implementare un tasto che apra il Grimorio (dialog già esistente, manca solo il trigger).

### 2. Fondamenta (bloccano altri task sotto)

- [x] Implementare un tooltip generico di una larghezza fissa e altezza auto con opzioni per evocarlo a destra, sinistra, sopra o sotto l'elemento target — serve al punto 3.
- [x] Aggiornare l'elenco delle fasi della partita: attesa, preparazione, raccolta, azione, incantesimo, fine — serve al punto 3 (oggi il modello `TurnPhase` ha solo raccolta/azione/conclusione).

### 3. Gameplay core

- [x] Board / componente vita: le fasi non sono più un elenco ma viene mostrata solamente quella in corso; un tooltip mostra tutte le fasi in ordine con il pallino illuminato su quella attiva (dipende dai punti 2).
- [x] Tootlip sulle carte avanzate: mostra quali elementi vanno combinati per ottenere la carta;
- [x] Tooltip su mazzo del giocatore (anche avversario) con conteggio carte rimanenti;
- [x] Tooltip su scarti del giocatore (anche avversario) con conteggio carte scartate;
- [x] Board: Nuovo icon button con icona question_mark che apre la dialog del regolamento + tooltip "Regolamento" (dipende dal punto 1) sotto Grimorio;
- [x] Board / componente vita: gli hp di un giocatore non sono un numero fatto e finito ma un oggetto che comprende max, current, shield. Normalmente rossa, la barra si svuota in base ai danni subiti e il contenitore resta scuro sotto. Se ho un valore di shield >= 1 accanto al rosso compare una barra azzurra che rappresenta la vita aggiuntiva. Il riempimento non va oltre la barra ma deve essere calcolato perché rimanga tutto dentro di essa.
- [x] Includere e implementare nel progetto il menu del cdk per gestire le azioni.

### 4. Gameplay polish (a costo zero, si appoggia a quanto appena fatto al punto 3)

- [x] Correggere: NG02955: The NgOptimizedImage directive (activated on an <img> element with the `ngSrc="http://localhost:4200/cards/fire.png"`) has detected that this image is the Largest Contentful Paint (LCP) element but was not marked "priority". This image should be marked "priority" in order to prioritize its loading. To fix this, add the "priority" attribute. — bug fix isolato, zero dipendenze, buono da togliere di mezzo subito.
- [x] Hover su una carta della fonte arcana illumina gli eventuali componenti presenti nella mano in quel momento. Si illuminano di azzurro le carte se non ho la formula completa, si illuminano di oro se posso combinarne abbastanza per ottenere la carta della fonte arcana. Uguale su residuo arcano. — riusa la stessa logica "ho le carte in mano?" appena scritta per il menu Combina (punto 3), quindi conviene farlo ora finché è fresco.
- [x] Migliorie grafiche varie: placeholder per pile scarti vuote (`DeckComponent`, nuovo input `emptyPlaceholder`) — `'boxed'` (riquadro tratteggiato vuoto, stesse dimensioni di una carta) per le pile con un proprio spazio dedicato (Fonte Arcana), `'text'` (solo testo, niente bordo) dove le carte già escono dal bordo del contenitore (scarti dei giocatori); label delle fasi nel `PhaseTrackerComponent` ora su sfondo a pillola, altrimenti si mescolavano con la linea di congiunzione dietro; foto profilo (Google, `referrerpolicy="no-referrer"`) accanto al nome in `player-hud.component`, con fallback all'iniziale come l'avatar della home; bordo dorato del mazzo comune in Raccolta passato da `box-shadow` incollato al bordo a un `outline` con `outline-offset` (aria reale) più pulsazione lenta (2.6s).
- [x] Ritocchi successivi: label/conteggio del placeholder `'boxed'` spostati sotto il riquadro (nel consueto `.deck__info`) invece che centrati dentro, per allinearsi a "Base"/"Avanzato"; nuovo input `showBorder` su `DeckComponent` per nascondere il bordo statico della carta quando un highlight esterno (l'outline pulsante del mazzo comune in Raccolta) lo marca già, evitando il doppio bordo; layout del pannello avversario specchiato punto per punto rispetto al giocatore (non solo capovolto in verticale) — ordine mazzo/scarti nella mano invertito, ordine punta/asta/manico invertito (manico verso il bordo schermo, punta adiacente alla mano, come già capitava per HUD/bacchetta che erano già ai lati opposti), e avatar/nome invertiti nell'HUD (`.hud__identity` con `flex-direction: row-reverse` quando `mirrored`). `PhaseTrackerComponent` non mostra più 'attesa' tra le tappe (resta comunque il primo valore di `TURN_PHASES` lato logica, mai il valore reale persistito) — con un solo tracker condiviso non aggiungeva informazione.
- [x] Anche gli scarti dell'area personale (giocatore e avversario) hanno ora un placeholder col riquadro tratteggiato, accettando che "esca" dal contenitore della mano come già fa il mazzo personale. Nuova variante `'boxed-text'` in `DeckComponent` (oltre a `'boxed'` e `'text'`): stesso riquadro di `'boxed'` ma con "Scarti 0" centrato dentro invece che in un `.deck__info` sotto — necessaria perché qui `showLabel`/`showCount` sono `false` (niente riga info propria a cui appoggiarsi, a differenza di "Base"/"Avanzato" in Fonte Arcana).
- [x] Bug fix: il bonus manico (1.4.3) spariva visivamente non appena la carta finiva in cima a una pila scarti — `DeckComponent` passava ad `app-card` solo l'elemento (`topElement`), mai il `manaBonus` della carta reale. Il dato non era mai stato perso (`Card.manaBonus` seguiva correttamente la carta), solo la sua vista in cima alla pila. Aggiunto un nuovo input `topManaBonus` a `DeckComponent`, propagato da `board.component.ts` per le uniche pile dove può capitare (scarti comuni/giocatore/avversario — non quelli avanzati, dato che il bonus si applica solo alle basi pescate dal mazzo comune). `topOf()` ora restituisce la `Card` intera invece del solo `Element`.

### 5. i18n & opzioni

- [x] Implementare i18n per avere multilingua (italiano + inglese) con italiano come lingua principale — prioritario da fare prima che il testo in giro nell'app cresca ancora (retrofit più costoso più si aspetta). Infrastruttura pronta (TranslationService + pipe + dizionari JSON); copertura reale finora solo sulla dialog Opzioni, il resto dell'app va tradotto pagina per pagina.
- [x] Home / opzioni: aggiungere nelle opzioni una select per la lingua e un toggle per l'audio (la select lingua dipende da i18n sopra; il toggle audio presuppone un sistema audio non ancora esistente, quindi per ora è solo uno stub visivo).

### 6. Profilo utente

- [x] Home / utente: aggiungere il pallino con immagine dell'utente loggato e menu contestuale con dialog. Il menu contiene anche "Esci" (rimosso il bottone separato dall'header, ora ridondante).
- [x] Home / utente (dialog): opzioni disponibili: cambia nome visualizzato, url immagine personalizzata, scegli dorso (tra quelli disponibili / sbloccati), elimina account (dipende dal punto sopra; "elimina account" è distruttivo, va implementato con conferma esplicita). "Elimina account" spostato qui dalla dialog Opzioni (niente più duplicati, un solo posto per la gestione dell'account).

### Prossime implementazioni (non ancora pianificate)

Superata dalla lista prioritizzata in cima al file — log eventi ed etichetta "Bacchetta comune" sono nel punto 7 lì, la ricarica del mana è risolta concettualmente dal Regolamento v2 (sez. "Motore di gioco" sotto).

## TODO — Motore di gioco (Regolamento v2)

Il regolamento è stato riscritto e finalizzato in [documentation/rulebook/v2/rules.md](documentation/rulebook/v2/rules.md). Il primo motore di turno reale è stato implementato secondo il piano in `.claude/plans/modular-cuddling-blanket.md` (nuovi `src/app/game/deck-builder.ts` e `src/app/game/turn-engine.ts`, più `src/app/services/game-engine.service.ts`): `board.component.ts` ora legge dati reali da Firestore invece di signal mock, e il bug per cui `setReady()` non faceva mai partire una partita reale (solo `createDebugGame` funzionava) è corretto. Restano fuori scope, rimandati a milestone successive: incantesimi oltre ai 4 stub a danno (e quindi anche l'applicazione di veleno/congelamento, oggi solo risolvibili una volta applicati — vedi sez. 3), tuono/lava, esplosione elementale, mana speciale, punta della bacchetta a runtime, hardening delle regole Firestore (oggi qualsiasi utente autenticato può scrivere lo stato di qualsiasi partita — accettato per questa milestone, nessuna transazione Firestore: solo il giocatore di turno scrive stato condiviso).

### 1. Fondamenta (bloccano tutto il resto)

- [x] Motore di turno reale con le 6 fasi del regolamento v2 (attesa, preparazione, raccolta, azione, incantesimo, fine). `game.model.ts` non ha più il `TurnPhase` locale stantio — importa `ActiveTurnPhase` da `turn-phase.model.ts` (le 5 fasi davvero persistibili; 'attesa' resta solo un valore di visualizzazione per chi non è di turno, mai scritto su Firestore). `board.component.ts` legge tutto da un `state` reale via `computed()`; resta da fare solo la risoluzione degli *effetti* di ogni fase (veleno/congelamento in Preparazione, incantesimi in Incantesimo — vedi sez. 2/3), non l'avanzamento in sé.
- [x] Sistema mana: rimossi `WAND_PIECE_MANA`/`BASE_TOTAL_MANA` da `wand.model.ts` e `mana`/`maxMana` da `PlayerState`; aggiunta `computePlayerMana(hand)` in `player.model.ts` (somma `ELEMENT_MANA` per carta) — non ancora chiamata da nessuna parte, dato che selezione/lancio incantesimi restano da fare. Nessun contatore "Mana: X" ambientale nell'HUD, come deciso.
- [x] Punti Salute: allineato a 20 (`PlayerState.hp`, regolamento v2 1.3). Rimosso `curseSlots` (non era una meccanica del regolamento v2) e il relativo effetto incantesimo `damage_cursed` da `SpellEffectType` — nessun incantesimo del catalogo lo usava.

### 2. Fasi del turno

- [x] Fase Raccolta (4.3): implementata in due passi persistiti su Firestore (`startCollect` pesca 2 e le mette in sospeso su `PlayerState.pendingCollect`, rimescolando gli scarti comuni se il mazzo è esaurito; `keepCard` risolve la scelta) — non un solo passo locale, perché un peek locale non scritto su Firestore si disallineerebbe dal mazzo vero se nel frattempo scattava un rimescolamento. La carta tenuta va negli scarti del proprio mazzo, non in mano.
- [x] Fase Finale (4.6): scarto totale della mano + pesca fresca di 5 carte, con rimescolamento automatico del proprio mazzo se necessario (`endTurn` in `turn-engine.ts`).
- [x] Fase Preparazione (4.2): risolta automaticamente in `endTurn` (`resolvePreparation` in `turn-engine.ts`), applicata a chi sta per iniziare il turno appena il turno passa di mano — 1 danno per livello di Avvelenamento e scioglimento (rimozione, non scarto) delle carte Congelamento in mano. Non serve un passo separato: l'unico modo di entrare in 'preparazione' è proprio questo handoff (o l'inizio partita, dove i contatori sono comunque a zero). Resta manuale solo l'*avanzamento* dalla fase — vedi "Automazione avanzamento fasi" sotto.
- [x] Rimescolamento mazzi (1.7): implementato in modo uniforme per tutti e tre i mazzi che si esauriscono in questa milestone — comune (Raccolta), avanzato (combinazione), proprio (fine turno) — via `drawUpTo()` in `deck-builder.ts`. Il decremento del livello di avvelenamento alla rimescolata del proprio mazzo resta da fare (dipende dall'avvelenamento, sez. 3).
- [x] Bonus manico (1.4.3): 10%/20% di possibilità di +1 mana, applicato singolarmente a ciascuna delle 2 carte pescate in Raccolta (non solo a quella tenuta) — `rollHandleBonus` in `turn-engine.ts`, chiamata da `startCollect`. L'incremento è permanente sulla carta pescata (`Card.manaBonus`, sommato in `computePlayerMana`), non un effetto temporaneo del giocatore — così segue la carta anche in caso di futuri furti. UI: `CardComponent` accetta ora un input `manaBonus`; nell'overlay di Raccolta il bonus resta nascosto ~900ms (già risolto nello stato, solo rivelato in ritardo lato UI) poi appare con un'animazione scale-up/down (`collect-mana-bonus` in `board.component.scss`) prima che il flusso prosegua. Anche la mano del giocatore ora porta `Card[]` invece di soli `Element[]`, per mostrare il bonus anche quando la carta boostata viene ripescata in mano in un turno successivo.
- [x] UI tracker di fase: nuovo `PhaseTrackerComponent` (`src/app/components/phase-tracker/`) — un'unica timeline condivisa con le 6 fasi in linea (pallino pieno per quelle passate, pulsante e acceso per l'attiva, vuoto per le future — tooltip con la descrizione su ognuna) più il bottone "Prosegui", posizionata sotto `.board__fonte-container` invece che nell'angolo isolato di prima. Sostituisce sia il vecchio indicatore di fase duplicato in ciascun HUD (solo tooltip al passaggio del mouse, rimosso da `player-hud.component`) sia il bottone assoluto in alto a sinistra — non più duplicazione tra pannello giocatore/avversario, dato che solo uno dei due ha davvero una fase attiva alla volta. Layout poi rivisto: pallino e label dei singoli step ora in riga (non più impilati) — si legge più allineato con l'etichetta "Turno di" e il bottone ai due lati; il nome del turno è troncato al primo spazio (nuova `firstNameOf()` condivisa in `player.model.ts`, usata anche da `player-hud.component`) dato che ora deve stare su un'unica riga stretta insieme al resto.
- [x] Automazione avanzamento fasi: `canAdvancePhase` in `board.component.ts` ora è `isPlayerTurn() && phase === 'azione'` — Azione resta l'unica fase manuale. Le altre 4 fasi persistite si auto-avanzano da sole via un `effect()` dedicato (`autoAdvanceReady`/`autoAdvanceDelayMs`, stesso schema del passthrough di debug per l'avversario finto, ma per il giocatore reale): Raccolta appena `hasCollectedThisTurn` diventa vero (500ms), Preparazione appena entrata dato che i suoi effetti sono già risolti in modo sincrono in `endTurn` (2000ms, per notare danno/scioglimento), Incantesimo appena entrata dato che non esiste ancora modo di lanciare incantesimi quindi è sempre "nessuno giocato" (500ms), Finale appena entrata dato che scarto+ripesca avvengono dentro `endTurn` stesso al momento dell'avanzamento (500ms). Attesa resta automatica per costruzione (mai persistita, sez. 1).

### 3. Meccaniche degli elementi

- [x] Fonte Arcana (2.6): `combineElements` in `turn-engine.ts` sostituisce il vecchio mock — le 2 basi vanno consumate negli scarti del mazzo comune (non semplicemente rimosse dalla mano, bug del mock originale), la carta ottenuta negli scarti del giocatore, lo slot si rimpiazza dal mazzo avanzato rimescolando i suoi scarti se esaurito (slot lasciato vuoto solo nel caso limite in cui anche gli scarti sono esauriti). Estesa con `combineSuperior` (2.4, formula fissa Fuoco+Acqua+Aria+Terra per Luce/Tenebra) — condivide con `combineElements` la logica di prendere/rimpiazzare lo slot (`takeFromFonte`), differiscono solo su quali/quante basi consumano dalla mano. Tooltip ricetta e bottone "Combina" ora coprono anche gli elementi potenti (`recipeTooltip`/`fonteMenuItems` in `board.component.ts`), non solo gli avanzati.
- [x] Avvelenamento (2.3.4): cap 0–3 livelli (`PlayerTokens.poison`). Risoluzione in `resolvePreparation` (1 danno per livello, sez. 2) e decadimento in `endTurn` (-1 livello quando `drawUpTo()` rimescola gli scarti del giocatore nel proprio mazzo). Applicazione (`applyPoison`, incrementa il livello cap a 3) pronta in `turn-engine.ts` ma senza ancora un chiamante reale — spetterà agli incantesimi col veleno come componente (rimandati, non ancora implementati).
- [x] Congelamento (2.3.1): nuovo `CardTier` `'freeze'` in `card.model.ts` per le non-carte generate a runtime (`applyFreeze` in `turn-engine.ts`, id via `crypto.randomUUID()`) — finiscono negli scarti del bersaglio e quindi possono essere pescate al posto di carte utili. Si sciolgono (rimosse, non scartate) in `resolvePreparation`. Le carte `'freeze'` sono escluse da `computePlayerMana` e non mostrano il badge Mana (`showMana` a false in `board.component.html`), dato che non sono carte giocabili — l'elemento `'ice'` riusato solo per l'illustrazione. Anche qui, `applyFreeze` non ha ancora un chiamante reale (spetta agli incantesimi col ghiaccio come componente, rimandati).
- [x] UI Avvelenamento/Congelamento: `player-hud.component` mostra ora un teschio (icona `poison` di `element.model.ts`, stesso stile "cerchio scuro + icona" del badge Mana delle carte) per ogni livello di Avvelenamento, accanto al valore vita dove prima stava il mana; la barra vita passa da rosso a verde malato quando il livello è > 0. Il Congelamento applica un overlay con la texture `public/images/freeze.png` (velata di azzurro, dato che di suo è quasi bianca) su tutto il box vita finché il giocatore ha ancora carte `'freeze'` non sciolte in mano, mazzo o scarti — non solo in mano, per riflettere il rischio persistente di pescarle. Entrambi gli indicatori sono attivi sia sul proprio pannello sia su quello dell'avversario (i dati non sono comunque protetti lato Firestore oggi). Overlay decorativo raddoppiato da uno span sr-only per l'accessibilità (WCAG 1.4.1).
- [ ] Nota: `applyPoison`/`applyFreeze` sono scritte e pronte ma non richiamabili da nessuna UI/meccanica oggi — esiste un sistema di Incantesimi (v1 minima, vedi punto 1 di "Prossimi passi"), ma nessuno dei suoi `SpellEffectType` risolti (`applySpellEffect` fa solo `damage`/`heal`) le innesca ancora. La risoluzione (danno/scioglimento) e il decadimento sono comunque reali e attivi da subito.
- [ ] Tuono che torna in fondo al mazzo invece che negli scarti (2.3.2), Lava che consuma carte/dà scudi (2.3.3) — non ancora specificati in dettaglio, restano per una fase successiva a Avvelenamento/Congelamento.
- [x] Esplosione elementale (2.4): nuova `resolveElementalExplosions` in `turn-engine.ts` — 1 danno quando Luce e Tenebra si trovano nello stesso luogo (solo al proprietario se in mano, a entrambi i giocatori se in Fonte Arcana), poi le 2 carte si consumano (spariscono, non scartate — evita di dover tracciare quali coppie sono "già esplose" per non farle ri-esplodere all'infinito a ogni controllo). In loop per il caso limite di più di 1 copia compresente. Richiamata dopo ogni azione che potrebbe introdurre un elemento potente in una mano o in Fonte Arcana: inizio partita (`tryStartGame`/`createDebugGame`, dato che la mano iniziale ha già 1 Luce + 1 Tenebra a testa, 1.2, e la Fonte Arcana appena rivelata potrebbe contenerli entrambi), `endTurn` (mano fresca di Finale), `combineElements`/`combineSuperior` (nuovo slot rivelato in Fonte Arcana da `takeFromFonte`).
- [x] Animazione "lampo + scossa" per l'Esplosione elementale (funzionava già ma non si vedeva nulla). Nuovi `GameState.explosionBatchId`/`lastExplosions` (`ExplosionEvent[]`, sostituiti non accumulati — segnale transiente per il client, non un log storico: il batch id incrementa solo quando succede davvero una nuova esplosione, così i client la distinguono da "niente di nuovo" anche quando il resto dello stato cambia per altri motivi). Tre pezzi, in base a cosa è visibile: **barra vita** (`player-hud.component`, sempre attivo, anche con la causa non visibile come la mano coperta dell'avversario) — nuovo input `damageEvent` (`{ id, amount }`, deduplicato per id) pilota un pulse rosso sul fill (`@keyframes hp-damage-flash`) più un numero "-N" che sale e svanisce sopra la barra (`@keyframes hud-damage-number`), 1s, calcolato in `board.component.ts` contando gli eventi dell'ultimo batch che coinvolgono quel ruolo; **carte in mano** (proprie) — l'effect di ghost-detection già esistente (vedi voce sopra sull'animazione di sparizione) ora riconosce anche Luce+Tenebra sparite insieme a un evento `location: 'hand'` per il proprio ruolo nell'ultimo batch non ancora processato (gate necessario per non scambiare un banale scarto di Finale per un'esplosione), animandole con `@keyframes hand-card-explode` (lampi via `filter: brightness()` + shake orizzontale via la proprietà standalone `translate`, non `transform` — già occupata dal posizionamento del ventaglio, stesso motivo per cui l'animazione di sparizione anima `top` invece di `transform`) prima di sparire, 700ms; **Fonte Arcana** — semplificato a un lampo + scossa sull'intera riga (`.board__fonte-row--exploding`, `@keyframes fonte-explode`, 500ms) invece di un ghost per singola carta, dato che quella riga usa `track $index` (posizionale) e non un id di carta stabile.
- [x] Composizione mazzo avanzato corretta (1.1): 20 carte (prima 22, mai confermato — placeholder arbitrario), 4 copie per ciascuno dei 4 elementi avanzati invece dello split ineguale 4/5/4/5. Aggiunta anche una meccanica non ancora scritta da nessuna parte: 1 copia di Luce e 1 di Tenebra partono già negli scarti del mazzo avanzato (non nel mazzo/Fonte da rimescolare) — bilancia la disponibilità totale, dato che ogni giocatore parte già con 1 copia a testa nel proprio mazzo personale (1.2). Regolamento (`rules.md` 1.1, 2.4) e testi in-app (`introduction.md`, `source.md`, `elements.md`) aggiornati di conseguenza; corretta anche un'imprecisione preesistente in `source.md` che diceva che le basi consumate in una combinazione si possono scartare "a scelta" (non è mai stata una scelta: si consumano sempre, vanno negli scarti del mazzo comune).
- [x] Carte "temporanee" generalizzate (2.3.1, 2.5): nuovo campo `Card.expiresAt?: ActiveTurnPhase` — se una carta con questo campo è ancora in mano quando si raggiunge quella fase, sparisce (mai scartata). Un'unica `resolveExpiringCards(hand, phase)` in `turn-engine.ts` sostituisce il vecchio filtro ad-hoc `card.tier !== 'freeze'`: Congelamento ora imposta `expiresAt: 'preparazione'` (`applyFreeze`), Residuo Arcano `expiresAt: 'fine'` (assegnato una volta per tutte in `buildResiduumDeck`, dato che vale per ogni copia). Agganciata sia in `resolvePreparation` (Congelamento) sia in `endTurn`, prima dello scarto della mano (Residuo — altrimenti finirebbe negli scarti del giocatore come una carta qualunque, cosa che "consumarsi" non è).
- [x] Residuo Arcano (2.5): nuova `combineResidue` in `turn-engine.ts` — combina 2 elementi base opposti (Fuoco+Acqua o Aria+Terra) dalla mano per ottenerne uno dal pool condiviso di 8 copie sempre scoperto (`GameState.residiumDeck`, un array semplice: nessuna pila scarti dedicata, dato che una copia consumata o scaduta non torna mai da nessuna parte — persa per il resto della partita). Va negli scarti del giocatore, come la carta ottenuta da `combineElements`/`combineSuperior` — non subito in mano: spendere 2 carte per ottenerne 1 sola d'uso immediato obbligato avrebbe spesso sprecato l'azione (mano rimasta scarsa, nessuna vera occasione di comporre altro nello stesso turno). Il "turno per utilizzarlo" (`expiresAt: 'fine'`) scatta da quando viene ripescato in una mano fresca, non da quando viene creato — bilanciamento discusso e rivisto rispetto alla prima versione (rulebook v2, 2.5, aggiornato di conseguenza). Il Residuo vale anche come **jolly**: nuova `removeOneByElementOrResidue` (ripiega su una carta `tier: 'residium'` in mano se l'elemento esatto manca) usata da `combineElements`, `combineSuperior` e dalla stessa `combineResidue` — se il jolly viene speso in una combinazione si consuma per sempre, non finisce negli scarti comuni come farebbe una base vera. UI: `hasAllBaseCards`/`residuoMenuItems`/`recipeTooltip` in `board.component.ts` sono ora jolly-aware per l'abilitazione dei bottoni "Combina"; l'evidenziazione della mano al hover resta invece solo sulla coppia esatta per Fonte Arcana ed elementi potenti (non ancora jolly-aware lì — gap noto, minore).
- [x] Le voci "Combina" (Fonte Arcana, elementi potenti, Residuo) spariscono del tutto fuori da Azione o quando il pool del Residuo è esaurito, invece di restare visibili ma disabilitate — la ricetta la spiega già il tooltip della carta stessa, un bottone permanentemente disabilitato non aggiungeva informazione. `ActionMenuComponent` non apre più un popup vuoto quando `items()` è vuoto (`[cdkMenuTriggerFor]="items().length > 0 ? menu : null"`).
- [x] Animazione di sparizione per le carte "temporanee" (Congelamento/Residuo, `Card.expiresAt`): si alzano e svaniscono (`@keyframes hand-card-vanish`, 1.8s) invece di sparire di scatto. Due meccanismi in `board.component.ts`, a seconda di chi controlla il momento della rimozione reale: **proattivo** per il Residuo (scade in Finale, che il proprio client auto-avanza da solo) — marcato "in sparizione" (`vanishingCardIds`) nello stesso istante in cui si programma il ritardo di auto-avanzamento, esteso apposta da 500ms a 1.8s quando in mano c'è una carta in scadenza, così l'animazione e il ritardo corrono in parallelo invece che in sequenza; **retroattivo** per il Congelamento (scade in Preparazione, risolta in modo atomico dentro l'`endTurn` di chi ha appena finito il turno — il proprio client la vede già sparita, mai "prima") — un `effect()` confronta la mano ad ogni cambiamento e, se una carta con `expiresAt` è sparita senza preavviso, la rimette a video come ghost (`vanishingGhosts`) nella sua vecchia posizione (indice/conteggio congelati a quel momento) finché l'animazione non finisce; il ritardo di 2s già esistente per Preparazione basta a contenerla senza bisogno di allungarlo. Le altre carte della mano restano sul consueto riflusso a 150ms (`transition: left/transform`, già presente) per richiudere lo spazio lasciato libero.
- [x] Bug fix (reale, non solo l'animazione): `GameEngineService.mutate()` faceva `getDoc` + `updateDoc` separati, senza transazione — verificato con un test isolato che la logica pura di scadenza (`resolveExpiringCards`/`endTurn` in `turn-engine.ts`) è corretta, quindi il problema era altrove. Due scritture concorrenti sullo stesso documento (es. il proprio auto-avanzamento e quello, quasi simultaneo, dell'avversario di debug) potevano correre in un classico read-modify-write: quella basata sullo snapshot più vecchio sovrascriveva l'altra, "resuscitando" stato già superato — bug osservato concretamente: un Residuo Arcano consumato a Finale tornava disponibile e veniva ripescato una seconda volta insieme a uno nuovo. Corretto avvolgendo `mutate()` in `runTransaction()` (Firestore riprova automaticamente la transazione se il documento cambia tra lettura e commit, invece di lasciar vincere l'ultima scrittura). `tryStartGame()` non è stato toccato: resta un getDoc+updateDoc semplice, ma la sua corsa (due client che avviano la partita quasi insieme) produce solo uno stato iniziale casuale leggermente diverso, non una perdita di dati — rischio molto più contenuto, lasciato così com'era.
- [x] Bug fix: dopo la transazione sopra, l'animazione di sparizione del Residuo a volte ripartiva da capo subito dopo essere arrivata la mano fresca, rivelando poi una carta diversa al suo posto. Causa: `vanishingCardIds` (marcatura proattiva) veniva ripulito da un timer locale indipendente, con la stessa durata nominale della rimozione reale (scrittura Firestore, quindi soggetta a latenza di rete) — se il timer locale scattava prima che la rimozione reale arrivasse, l'effect retroattivo (quello per il Congelamento, sez. sopra) non trovava più l'id in `vanishingCardIds` e trattava la sparizione come "a sorpresa", ri-animandola da capo come ghost. Rimosso il timer indipendente: `vanishingCardIds` ora si ripulisce solo dentro l'effect retroattivo, nello stesso momento in cui osserva la rimozione reale — un solo punto di verità, niente più corsa fra i due. Durata dell'animazione portata da 1.8s a 1s (`VANISH_DURATION_MS`, `@keyframes hand-card-vanish`).
- [x] Mana speciale (3.2.x): design rivisto rispetto al vecchio "lo pesco e mi succede qualcosa" — l'effetto è ora legato alla carta stessa e scatta solo quando viene *spesa per pagare un incantesimo* (`Card.specialMana?: 'prismatic' | 'vital' | 'chaotic'`, nuovo tipo in `card.model.ts`). 6 carte casuali del mazzo comune (2 copie ciascuno) lo ricevono in `applySpecialMana` (`deck-builder.ts`), chiamata da `createInitialGameState`. **Prismatico**: +1 mana permanente sul proprio valore, sempre (`computePlayerMana` in `player.model.ts`, si somma a `manaBonus` del bonus manico se presente). **Vitale/caotico**: dato che il motore non ha un concetto generico di "bersaglio della magia" (ogni `SpellEffectType` ha il proprio target cablato — `damage` colpisce sempre l'avversario, `heal` cura sempre chi lancia, vedi punto 1 di "Prossimi passi"), l'aggancio naturale è lì: vitale aggiunge 2 PS extra a un effetto `heal` presente nella magia pagata, caotico aggiunge 2 danni extra a un effetto `damage` — se la magia non ha l'effetto corrispondente, la carta vale come mana comune, nessun effetto. Il bonus va calcolato in `castSpell` (Azione), non in `resolveSpells` (Incantesimo): le carte di pagamento vengono scartate subito, quindi non sarebbero più consultabili al momento della risoluzione — nuovo `PendingSpell` (`player.model.ts`, `{ card, vitalBonus, chaoticBonus }`) sostituisce il vecchio `PlayerState.pendingSpells: Card[]` per portare il bonus con sé fino a lì. UI: nuovo badge circolare più piccolo (icone `diamond_shine`/`favorite`/`explosion` già presenti in `public/icons/`) impilato in colonna sotto il valore di mana e allineato al suo stesso bordo sinistro (non centrato, per via della differenza di dimensione) — un anello colorato per tipo (oro/verde/rosso) invece di ricolorare le SVG monocromatiche. Aggiunto a tutti i punti che mostrano carte reali di mano: mano propria, ghost di sparizione, overlay di Raccolta, dialog di pagamento incantesimo (`CastSpellDialogComponent`).
- [x] Ritocchi al badge mana speciale, in due passate. **Bug fix**: l'icona risultava invisibile — `padding: 12%` su un badge largo `16%` con `box-sizing: border-box` lasciava un'area di contenuto negativa (12+12 > 16). **Stile**: sostituito l'anello colorato attorno a un'icona grigia con l'icona stessa colorata via maschera CSS (`mask`/`-webkit-mask` + `background-color: currentColor`, badge diventato uno `<span role="img">` invece di un `<img>`, dato che le SVG sorgenti sono monocromatiche) — l'`inset` del pseudo-elemento `::before` mascherato è ora in percentuale della dimensione del badge stesso, non dell'intera carta, quindi non ripete il bug. Badge poi ingrandito da `16%` a `24%` della larghezza carta su richiesta diretta. **Bug fix separato**: lo stesso mana speciale spariva visivamente non appena una carta finiva in cima a una pila di scarti — stesso bug già visto col bonus manico (`topManaBonus`), stavolta mancava l'equivalente `topSpecialMana` su `DeckComponent`; aggiunto e propagato da `board.component.ts` per le 3 pile dove può capitare (scarti comuni/giocatore/avversario).
- [x] Dialog di scelta per "Combina" (regolamento 2.3/2.4/2.5): `combineElements`/`combineSuperior`/`combineResidue` (`turn-engine.ts`) consumavano sempre la prima carta valida trovata in mano per ogni elemento richiesto — rischio concreto di perdere per sbaglio una copia con bonus manico o mana speciale quando in mano ce n'era anche una semplice. Nuova `combineNeedsChoice(hand, element)`: true se per quell'elemento ci sono 2+ copie esatte (tier `'base'`) e almeno una porta qualcosa di prezioso, oppure se una base esatta e un Residuo Arcano sono ENTRAMBI disponibili (vedi bug fix sotto — un Residuo è comunque una risorsa scarsa da proteggere, anche contro una base del tutto semplice). Se non c'è scelta da fare su nessun elemento richiesto, si combina subito come sempre (zero interruzioni nel caso comune): `board.component.ts` (`resolveCombineChoice`) apre la nuova `CombineDialogComponent` solo quando serve davvero, prima di chiamare `combineAdvanced`/`combineSuperior`/`combineResidue`. I tre reducer accettano ora un `chosenIds?: Partial<Record<BaseElement, string>>` opzionale (elemento → id carta scelta) che, quando presente, sostituisce la ricerca automatica (`removeOneByElementOrResidue` non ricade mai silenziosamente su un'altra carta se la scelta esplicita non corrisponde — accetta sia l'id di una base esatta sia quello di un Residuo). Stesso guscio "dialog CDK, lista di `app-card`, conferma/annulla" di `CastSpellDialogComponent`, ma componente separato: la logica di selezione è diversa (qui un pick per elemento con scelta, non un multi-select libero su una soglia di mana). Un Residuo Arcano può comparire come candidato in più di uno slot contemporaneamente (il pool è condiviso tra tutti gli elementi, a differenza delle basi che appartengono a un solo elemento) — `isTaken()` lo esclude dagli altri gruppi non appena scelto per uno, così non si rischia di spenderlo due volte.
- [x] Bug fix: un Residuo Arcano in mano non veniva mai evidenziato (oro/blu) quando si passava il mouse su un elemento avanzato o potente della Fonte Arcana, nonostante potesse davvero completare la formula al posto di una base mancante (2.5). Causa doppia in `board.component.ts`: `handCardHighlight` filtrava via ogni carta non-base PRIMA ancora di valutarla (quindi un Residuo non veniva mai considerato, in nessuno dei 3 tipi di hover — avanzato, potente, Residuo stesso), e il template ripeteva lo stesso filtro `card.tier === 'base'` sulle classi CSS, escludendo il Residuo una seconda volta anche se la funzione avesse risposto qualcosa. Riscritta per accettare la carta intera (non solo il suo elemento) e considerare un Residuo come jolly per qualunque elemento mancante nella formula in hover, con lo stesso criterio "gold se la combinazione è completabile subito (contando gli eventuali Residuo disponibili), blue se ne fa parte ma non basta ancora" già usato per le basi esatte.
- [x] Tooltip mana speciale sulla carta: quarto `ng-template` in `board.component.html` (`#specialManaTip`, accanto a `#spellTip`/`#recipeTip`/`#residuoTip`, tutti condividono le classi `.rich-tooltip__*`) — nome e icona sulla stessa riga (`display: flex; justify-content: space-between`), poi `<hr>`, poi la spiegazione testuale dell'effetto (nuove chiavi i18n `card.specialManaEffect.{prismatic,vital,chaotic}`, non ancora esistevano: fino ad ora solo il badge/aria-label comunicavano l'effetto, senza spiegarlo a chi non conosce già la regola). L'icona nel tooltip riusa la stessa tecnica di ricolorazione via maschera CSS già vista sul badge di `CardComponent` (le SVG sorgente sono monocromatiche) — duplicata invece di condivisa: sono due componenti diversi e il progetto non usa mixin SCSS da nessuna parte.

### 4. Testi del regolamento in-app

- [x] Riscritti `public/config/rulebook/it/*.md` (introduction, turn, source, mana, elements, wand) sul contenuto di `documentation/rulebook/v2/rules.md`. Aggiunto un settimo file, `spells.md` (Incantesimi + Regola d'oro), assente prima — nessuno dei 6 file copriva la sezione 5 del regolamento (creare/lanciare incantesimi) né la Regola d'oro. `public/config/regolamento.json` aggiornato con la nuova voce e un ordine di navigazione più didattico (Introduzione → Turno → Elementi → Fonte Arcana → Mana → Bacchetta → Incantesimi).

### Note aperte (bassa priorità, da tenere a mente)

Superata dalla lista prioritizzata in cima al file (punti 7-9) — tenuta qui solo come indice: loader al refresh, rimozione dell'auto-avanzamento di debug, e l'opzione "scarta invece di subire danno" per l'esplosione elementale sono lì.

## TODO — Design carta (Design B)

- [x] Ridisegnare `card.component`: due sezioni tonde piene (non più solo icona centrata) integrate nel bordo, in alto a sinistra il valore di Mana e in basso a destra l'icona dell'elemento (l'icona resta un aiuto per l'accessibilità, non l'informazione primaria). Bordo e sezioni a opacità piena per restare leggibili sopra qualsiasi illustrazione. Aggiungere un `box-shadow` inset (accanto a quello esterno già usato per l'impilamento tra carte sovrapposte) che proietti una leggera ombra del bordo sull'illustrazione sottostante, per un effetto "scatola". Il badge Mana in alto a sinistra è la scelta deliberata: nel fan della mano le carte si overlappano al 58–80% (`board.component.ts`) con z-index crescente da sinistra a destra, quindi solo la striscia sinistra di ogni carta resta visibile — un badge in quell'angolo resta leggibile anche a mano piena, cosa che un'icona centrata non garantirebbe.